---
name: Hello 
# runs of trigger
on:
  push:
    branches:
      - main
jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: say hello to user
        run: echo "hello Dost"
      - name: say hello to username
        run: echo "hello $user"
